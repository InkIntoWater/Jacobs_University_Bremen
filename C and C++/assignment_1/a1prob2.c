#include <stdio.h>
int main()
{
    int result;
    // The result of our calculation
    result = (2 + 7) * 9 / 3;
    printf("The result is %d\n", result);
    return 0;
}
//We needed to provide argument i.e. result in the line 7.